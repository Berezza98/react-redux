import React from 'react';

import './Header.css';

const Header = () => {
    return (
        <header className="todo-header">
            <h4>To Do Application</h4>
        </header>
    );
};

export default Header;